================================================================================
  EASIEST WAY TO RUN YOUR BOT ONLINE (PythonAnywhere – free)
================================================================================

Signals only: 7:00–11:00 AM EST (session is fixed; no alerts outside this window).

Replace YOUR_USERNAME with your PythonAnywhere username everywhere below.

--------------------------------------------------------------------------------
STEP 1 – Bash console: clone and install (run once)
--------------------------------------------------------------------------------

git clone https://github.com/ahzam0/nasdaq100bott.git
cd nasdaq100bott/mnq_bot
python3.10 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

--------------------------------------------------------------------------------
STEP 2 – Web tab: create app
--------------------------------------------------------------------------------

1. Web → Add a new web app → Flask → Python 3.10
2. Code directory:  /home/YOUR_USERNAME/nasdaq100bott/mnq_bot
3. Virtualenv:      /home/YOUR_USERNAME/nasdaq100bott/mnq_bot/venv
4. Open "WSGI configuration file" and REPLACE ALL with:

import sys
sys.path.insert(0, '/home/YOUR_USERNAME/nasdaq100bott/mnq_bot')
from run_bot_pa import application

5. Save, then click the green RELOAD button.

--------------------------------------------------------------------------------
STEP 3 – Set Telegram webhook (do once)
--------------------------------------------------------------------------------

Option A – In browser (easiest): open this URL (replace YOUR_USERNAME):
  https://YOUR_USERNAME.pythonanywhere.com/set-webhook?secret=mnqbotcron123
  The page will say "Webhook set to ...". Done.

Option B – In Bash:
  curl -F "url=https://YOUR_USERNAME.pythonanywhere.com/webhook" "https://api.telegram.org/bot8510793606:AAE553KsIe0E6rAskRN-fUqM0H57_UN92zY/setWebhook"

--------------------------------------------------------------------------------
STEP 4 – Tasks tab: scan every minute (PythonAnywhere only)
--------------------------------------------------------------------------------

1. Tasks → Add a new task
2. Type: Run a URL
3. URL: https://YOUR_USERNAME.pythonanywhere.com/cron/scan?secret=mnqbotcron123
4. Schedule: Every minute
5. Save

(On Railway/Render the app runs the scan every 60 sec inside the app – no task needed.)

--------------------------------------------------------------------------------
BONUS – Free account: whitelist (Account or Network tab)
--------------------------------------------------------------------------------

Add:  api.telegram.org
Add:  query1.finance.yahoo.com

--------------------------------------------------------------------------------
Live price without WebSocket (default: stable on Railway, etc.)
--------------------------------------------------------------------------------

By default the bot uses Yahoo REST for live price (updated every scan).
This uses Yahoo REST only (no WebSocket), so the bot won’t hit thread limits.
You still get live NQ price and full signals. For WebSocket: MNQ_YAHOO_WS_REALTIME=true

================================================================================
DONE. Open Telegram and send /start to your bot.
Live signals only during 7:00–11:00 AM EST.
================================================================================
